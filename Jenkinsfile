pipeline {
    agent any{
    }
    stages {
        stage('Build') {
         steps {
            echo "welcome maven"
         }stage('test'){
            echo "welcome to test"
         }stage('sonar'){
            echo "welcome to sonar"
         }
        }
    }
}
